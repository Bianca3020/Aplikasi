<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HopeWave">
        <activity
            android:name=".CheckoutActivity"
            android:exported="false" />
        <activity
            android:name=".ExitActivity"
            android:exported="false" />
        <activity
            android:name=".NotaActivity"
            android:exported="false" />
        <activity
            android:name=".BakeryActivity"
            android:exported="false" />
        <activity
            android:name=".HasilFormActivity"
            android:exported="false" /> <!-- ⭐ HOME PAGE = DEFAULT ACTIVITY (Launcher) -->
        <activity
            android:name=".HomePageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Activity lainnya -->
        <activity
            android:name=".FormActivity"
            android:exported="false" />
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".CalculatorActivity"
            android:exported="false" />
        <activity
            android:name=".KonversiActivity"
            android:exported="false" />
    </application>

</manifest>